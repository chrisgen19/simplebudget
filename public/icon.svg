<svg width="512" height="512" viewBox="0 0 512 512" xmlns="http://www.w3.org/2000/svg">
  <!-- Background -->
  <rect width="512" height="512" rx="115" fill="#1e293b"/>

  <!-- Wallet icon -->
  <g transform="translate(256, 256)">
    <!-- Wallet body -->
    <rect x="-120" y="-80" width="240" height="160" rx="20" fill="#f8fafc" stroke="#64748b" stroke-width="8"/>

    <!-- Wallet flap -->
    <path d="M -120 -60 Q -120 -80 -100 -80 L 100 -80 Q 120 -80 120 -60 L 120 -40 L -120 -40 Z" fill="#cbd5e1"/>

    <!-- Card slot -->
    <rect x="-90" y="-20" width="180" height="80" rx="10" fill="#1e293b" opacity="0.1"/>

    <!-- Money symbol -->
    <text x="0" y="35" font-family="Arial, sans-serif" font-size="80" font-weight="bold" fill="#10b981" text-anchor="middle">₱</text>
  </g>
</svg>
